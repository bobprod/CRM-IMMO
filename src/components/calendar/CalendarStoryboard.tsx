import CalendarModule from "@/components/calendar/CalendarModule";

export default function CalendarStoryboard() {
  return (
    <div className="bg-white min-h-screen">
      <CalendarModule language="fr" />
    </div>
  );
}
